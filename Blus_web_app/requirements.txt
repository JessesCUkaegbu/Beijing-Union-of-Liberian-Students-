# ── Production dependencies ────────────────────────────────────────────────────
# Install: pip install -r requirements.txt
# Dev extras: pip install -r requirements-dev.txt

Django==6.0.5
asgiref==3.11.1

# Server
gunicorn==25.0.0

# Database
psycopg2-binary==2.9.11     # PostgreSQL adapter (Railway)
dj-database-url==3.1.2      # DATABASE_URL parsing helper

# Environment & config
django-environ==0.13.0

# Static files
whitenoise==6.12.0

# API
djangorestframework==3.17.1
djangorestframework-simplejwt==5.5.1
django-cors-headers==4.9.0

# Auth
PyJWT==2.12.1
django-ratelimit==4.1.0     # brute-force/signup-abuse throttling on login & register

# Admin theme
django-jazzmin==3.0.5

# Images
Pillow==12.2.0

# Media storage (optional — falls back to local disk if unconfigured, see .env.example)
django-storages==1.14.6
boto3==1.43.59

# Utilities
sqlparse==0.5.5
tzdata==2026.2
packaging==26.2
